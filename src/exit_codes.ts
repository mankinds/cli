/**
 * Documented exit codes for the CI runner. Imported by callers
 * instead of hard-coding integers.
 */

export const EXIT_OK = 0;
export const EXIT_GATE_FAILED = 1;
export const EXIT_INFRA_ERROR = 2;
